#!/bin/bash
sudo apt install screen -y
SCRIPT="$HOME/e.sh"

if pgrep -f "bash $SCRIPT" > /dev/null; then
    echo "e.sh sudah berjalan. Keluar..."
    exit
else
    echo "Menjalankan e.sh..."
    screen -S lol -dm bash "$SCRIPT"
fi
